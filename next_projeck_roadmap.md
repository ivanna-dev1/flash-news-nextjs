# 🗺 План проекту: FlashNews (Next.js News Portal)

Цей файл є єдиним джерелом істини для проекту FlashNews. План базується на досвіді попереднього проекту (Online Store), але з урахуванням особливостей Next.js та професійних підходів.

## 🛠 Стек технологій

- **Next.js** (App Router)
- **Tailwind CSS v4**
- **News API / GNews API** (для отримання реальних новин)
- **LocalStorage** (для збереження закладок)

---блок1

# 🟦 МОДУЛЬ 1: ТЕ ЩО МИ ВЖЕ ЗРОБИЛИ (Completed)

- **Крок 1:** Ініціалізація проекту за допомогою `npx create-next-app@latest .` (з прив'язкою до існуючої папки). ✓
- **Крок 2:** Вибір правильних налаштувань: JavaScript, ESLint, Tailwind CSS, App Router, src/ directory. ✓
- **Крок 3:** «Велике прибирання» — очищення `globals.css` (тільки Tailwind директиви) та `page.js` (пуста функція `Home`). ✓
- **Крок 4:** Перший запуск сервера через `npm run dev`. ✓

---

# 🟦 МОДУЛЬ 2: ГЛОБАЛЬНА СТРУКТУРА ТА UI

- **Крок 5: Розподіл на компоненти (ПРІОРИТЕТ):**
  - Створення папки `src/components`.
  - Створення `Header.jsx` з логотипом та навігацією.
  - **Реалізація "Активних посилань" (Active Links):** візуальне виділення поточної сторінки в меню за допомогою `usePathname` (це сучасний підхід у Next.js).
  - Створення `Footer.jsx`.
- **Крок 6: Навігація через Layout:**
  - Підключення `Header` та `Footer` у файл `src/app/layout.js`, щоб вони автоматично з'являлися на всіх сторінках.
- **Крок 7: Константи та Початкові стани:**
  - Створення файлу `src/constants/initialState.js` для винесення початкових значень (наприклад, для стану пошуку або фільтрів: `INITIAL_FILTERS`, `INITIAL_SEARCH_DATA`).
- **Крок 8: Адаптивність головного контейнера:**
  - Заміна фіксованих широт (напр. `w-230`) на `w-full` з `max-w-` та оптимізація відступів (padding) для мобільних.
- **Крок 8.1: Темна тема (Dark Mode):** 🆕
  - Реалізація перемикача між світлою та темною темами за допомогою Tailwind CSS.
  - Збереження обраної теми в `localStorage`, щоб вона не зникала після перезавантаження сторінки.

---

блок2

# 🟦 МОДУЛЬ 3: ВІДОБРАЖЕННЯ НОВИН (News Feed)

- **Крок 9: Компонент `NewsCard`:**
  - Верстка картки новини.
  - **Важливо:** Використання `flex-1` або `grid` замість жорстко заданих розмірів (`w-1/2` тощо) для уникнення проблем на різних пристроях.
  - **Важливо:** Обов’язкові заглушки (Placeholder) для картинок та тексту (наприклад, дефолтна картинка чи напис "Фото відсутнє"), якщо дані не прийшли від API.
- **Крок 10: Робота з зображеннями:**
  - Використання компонента `next/image` для оптимізації швидкості завантаження картинок.
- **Крок 11: Головна сторінка (`page.js`):**
  - **Інтерактивний елемент (Слайдер):** Створення каруселі або слайдера для "Гарячих новин" (Top Stories) вгорі сторінки для покращення залученості.
  - Відображення списку звичайних новин через `.map()`.
  - Реалізація "скелетонів" (Skeletons) — візуальних заглушок під час завантаження даних (Loading state).
  - **Пагінація:** Додавання кнопок переходу по сторінках або кнопки "Завантажити ще" (Load More) при гортанні списку новин.
- **Крок 11.1: UX-фішки для читання:** 🆕
  - **Reading Progress Bar:** тонкий індикатор вгорі сторінки, що показує, скільки статті вже прочитано.
  - **Scroll to Top:** кнопка, що з'являється при прокрутці вниз і дозволяє миттєво повернутися вгору.

---

# 🟦 МОДУЛЬ 4: ФУНКЦІОНАЛ ТА API (Отримання та обробка даних)

- **Крок 12: Робота з API:**
  - Налаштування `fetch` запитів до News API.
  - Обробка станів `loading` та `error` (якщо API не відповідає).
- **Крок 13: Пошук та Фільтрація:**
  - Реалізація пошукового інпуту.
  - Використання **єдиного об'єктного стану** (наприклад, `searchParams`) для параметрів запиту/пошуку.
  - Оптимізація перевірки застосування фільтрів через `Object.entries().some()`.
  - Спрощення логіки розрахунку відображуваних новин.
- **Крок 14: Категорії:** Фільтрація новин за тематиками (Технології, Спорт, Бізнес).

---

# 🟦 МОДУЛЬ 5: ДИНАМІЧНИЙ РОУТИНГ (Деталі новини)

- **Крок 15:** Створення динамічного маршруту `src/app/news/[id]/page.js` для окремої новини.
- **Крок 15.1: Breadcrumbs (Хлібні крихти):**
  - Реалізація компонента навігації (наприклад: `Головна > Категорія > Назва новини`) для зручного переміщення між сторінками, як на реальних масштабних сайтах.
- **Крок 16:** Отримання `id` новини та відображення повного тексту статті з API.
- **Крок 17:** Форматування дати публікації (напр. "2 години тому", використання `Intl.DateTimeFormat`).
- **Крок 17.1: Соціальний шарінг (Sharing):** 🆕
  - Додавання кнопок під заголовком статті для швидкого поширення новини у Facebook, Telegram, Twitter (X).
  - Це допоможе зрозуміти, як працюють посилання на різні платформи.

---

# 🟦 МОДУЛЬ 6: ПЕРСОНАЛІЗАЦІЯ (Bookmarks)

- **Крок 18:** Логіка "Додати в закладки" через `LocalStorage`. Збереження улюблених новин.
- **Крок 19:** Окрема сторінка `src/app/bookmarks/page.js` для перегляду збережених новин.
- **Крок 19.1: Доступність (Font Size Changer):** 🆕
  - Можливість змінювати розмір шрифту статті для комфортного читання (Збільшити/Зменшити).

---

# 🟦 МОДУЛЬ 7: ПОЛІРУВАННЯ ТА SEO

- **Крок 20: SEO Оптимізація та Доступність:**
  - Додавання динамічних `meta title` та `description` для кожної сторінки новин (через Metadata API в Next.js).
  - **Open Graph (OG):** Налаштування метатегів для соцмереж, щоб при поширенні статті була гарна картинка та заголовок.
  - Перевірка доступності (Alt-теги для фото, семантика HTML).
  - Дописати заглушки всюди, де може пропасти контент.
- **Крок 21: Фінальна адаптивність (ОБОВ'ЯЗКОВО):**
  - Глобальна перевірка та виправлення розмірів `w-` та `h-` у всіх місцях, де це може призвести до проблем з відображенням на різних пристроях. Уникати хардкодних широт.
  - Окрема увага до мобільних телефонів: додавання "бургер-меню" до навігації у `Header.jsx`, налаштування сіток (`grid-cols-1` для смартфона, `grid-cols-3` для ПК).
- **Крок 22:** Деплой проекту.

---

## 💡 Нотатки для навчання (Learning Notes)

- **App Router**: Розуміння різниці між `layout.js` та `page.js`.
- **Server Components**: Розуміння того, як Next.js рендерить сторінки за замовчуванням на сервері, і коли нам потрібно використовувати `'use client'` (наприклад, для `useState` у пошуку).
- **Компонентний підхід**: Розділення відповідальності, винесення логіки та констант (`INITIAL_STATES`).
- **Стійкість UI**: Завжди передбачати що зображення може бути битим, а текст не прийти, щоб дизайн "не ламався" (Флекс-бокси, заглушки).
